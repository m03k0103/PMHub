# PMHub 共通用語・概念定義集 (TERMINOLOGY.md)

本ドキュメントは、PMHub（政策会議ウォッチ）プロジェクトにおいて作業を行う人間（ユーザー）と AI Agent（ペアプログラミングアシスタント）の双方が**常に同一の認識（On the same page）**を持ち、認識の齟齬・コミュニケーションミス・コード/データ設計のブレを根絶するための**公式用語・概念辞書（Single Source of Truth）**です。

---

## 1. 最重要原則：1概念1用語の徹底（中黒併記の禁止）

- 「会議・開催回」「資料・配付資料」「会議体・会議」のように、**中黒（・）やスラッシュ（/）で複数の概念を曖昧に併記してはならない**。
- すべての対話、実装計画（Implementation Plan）、成果報告（Walkthrough）、コード、コミットメッセージにおいて、**1つの概念に対して必ず定められた1つの確定用語を使用すること**。

---

## 2. 確定用語対照表（Glossary Master）

| 確定用語（日本語） | 英語表記 / コード内キー | 定義・役割・格納場所 | 識別IDフォーマット |
| :--- | :--- | :--- | :--- |
| **会議体** | Council (`councils`) | 審議会・検討会・懇談会・委員会・推進本部等の**親組織マスター**。<br>「第X回」などの回次を含まない親名称のみを1会議体につき1レコードで管理。 | `{省庁略称}-{会議体スラグ}`<br>（例: `cas-kokudo_kyoujinka`, `cao-ai_hq`）<br>※ハイフン厳格1個・2セグメント |
| **開催回** | Meeting (`meetings`) | 会議体の**個別の開催会合**。<br>第1回、第2回、臨時会等。親の `councilId` に紐付く。 | `{省庁}-{スラグ}-{YYYYMMDD}-{回次}`<br>（例: `cas-kokudo_kyoujinka-20260703-024`）<br>※ハイフン厳格3個・4セグメント |
| **会議体資料** | Council Material (`council.materials`) | 会議体マスターに紐付く、委員名簿や設置要綱・運営規則などの**組織常設資料**。 | 会議体オブジェクト内の `materials` 配列 |
| **配付資料** | Material (`meeting.materials`) | 各開催回に提出・配布された、議事次第・説明資料・議事録等の**各回末端PDF/HTML資料リンク**。 | 開催回オブジェクト内の `materials` 配列 |
| **手動保護** | Manual Lock (`manualLock: true`) | 人間またはAIが手動で追加・修正した会議体・開催回・配付資料を、クローラーの自動上書き・再配分・削除から**100%保護するフラグ**。 | 対象オブジェクトの `"manualLock": true` |
| **スクレイピングルール** | Scraping Rule (`scrapingRules`) | 各会議体における次回の最新開催回・配付資料を自動検知するための**抽出正規表現・セレクター定義**。 | `docs/data.json` の `scrapingRules[councilId]` |
| **ディスカバリー** | Discovery (`discover_councils.py`) | 省庁の審議会等一覧URLを巡回し、未登録の新規「会議体」を自動検知するエンジン。 | 管理コンソール TAB 1 (審議会ディスカバリー) |
| **クローラー** | Crawler (`crawler.py`) | 登録済み「会議体」の公式URLを巡回し、最新の「開催回」および「配付資料」を自動収集・同期するエンジン。 | 管理コンソール TAB 1 (会議・資料自動収集) |
| **却下会議体** | Rejected Council (`rejected_councils.json`) | ディスカバリーで検出されたが会議体ではないリンク（報道発表一覧、他省庁ポータル等）を永続的に除外・再検出防止するリスト。 | `admin/rejected_councils.json` |
| **ロールバック** | Rollback (`POST /api/rollback-data`) | `admin/backups/` 配下に自動保存された過去30世代から、直前世代の `data.json` を安全に復元する機能。 | 管理コンソール TAB 1 のロールバックボタン |
| **開催日不明** | Unconfirmed Date (`2099/01/01`) | クローラーで最新開催回を検知した際、開催日が特定できない場合に設定する**要確認ダミー日付**（当日日付設定は禁止）。 | `"date": "2099/01/01"` |
| **実装計画** | Implementation Plan (`implementation_plan.md`) | コード・データ修正作業前に作成・提示し、ユーザーの事前承認を得るための設計書（**Proceedボタン必須**）。 | アーティファクト (`RequestFeedback: true`) |
| **成果報告** | Walkthrough (`walkthrough.md`) | 開発・テスト完了後に実施内容と検証結果をユーザーに報告する完了ドキュメント。 | アーティファクト (`UserFacing: true`) |
| **レートリミット** | Rate Limit (0.35秒スロットリング) | 行政サーバー負荷軽減およびWAF遮断防止のため、リクエスト間に必ず挟む待機時間（**最低0.35秒以上**）。 | クローラー通信ループ (`time.sleep(0.35)`) |

---

## 3. 主要エンティティの詳細定義とID体系

### 3.1. 会議体（Council）
- **定義**: 恒久組織・時限組織を問わず、省庁が設置する審議会、検討会、委員会、分科会、部会、推進本部、懇談会等の**母体組織マスターレコード**。
- **データ構造**: `docs/data.json` の `councils` 配列に格納。
- **原則**: **1会議体につき必ず1レコード**。名称に「第X回」などの開催回次を含めてはならない（親名称に正規化）。
- **ID体系（厳格）**: `{省庁略称}-{会議体スラグ}`（**ハイフン厳格1個・2セグメント**）。スラグ内の単語区切りには `_`（アンダースコア）を使用（例: `cas-kokudo_kyoujinka`, `meti-sankoshin`）。

### 3.2. 開催回（Meeting）
- **定義**: 各会議体が実際に開催した**個別の会合レコード**（第1回、第2回、第3回、臨時会等）。
- **データ構造**: `docs/data.json` の `meetings` 配列に格納。各開催回は必ず親会議体の `councilId` を保持する。
- **ID体系（厳格）**: `{省庁略称}-{会議体スラグ}-{日付8桁YYYYMMDD}-{回次000またはs00}`（**ハイフン厳格3個・4セグメント**）。
  - 通常回次: 数字3桁ゼロ埋め（例: `cas-kokudo_kyoujinka-20260703-024`）
  - 臨時会・回次なし: `s` + 数字2桁（例: `cas-tokubetsu-20260807-s01`）

### 3.3. 会議体資料（Council Material）
- **定義**: 開催回（各回）ではなく、**会議体そのものに恒常的に紐付く公式資料**（委員名簿、設置要綱、運営規程など）。
- **データ構造**: 会議体オブジェクトの `materials` 配列に格納（例: `council.materials`）。

### 3.4. 配付資料（Material）
- **定義**: 各開催回において配付・提出・公開された**個別の資料データ**（議事次第、説明資料、参考資料、議事要旨、議事録等のPDF/HTML末端リンク）。
- **データ構造**: 開催回オブジェクトの `materials` 配列に格納（例: `meeting.materials`）。
- **原則**: 中間ナビゲーションリンクではなく、末端のPDF/HTMLリンクを直接取得して登録する。

---

## 4. データ保護と運用フラグ

### 4.1. 手動保護（`manualLock: true`）
- 人間またはAIエージェントが手動で追加・修正した会議体、開催回、配付資料には、必ず `"manualLock": true` を付与する。
- `manualLock: true` が設定されたレコードは、クローラー（`crawler.py`）の重複排除・再配分ロジックや `apply_report.py` による自動上書きから除外され、**絶対に破壊・改変されない**。

### 4.2. 開催日不明（`2099/01/01`）
- クローラーが最新の開催回を検知した際、開催日を抽出・特定できない場合に、**当日日付（`datetime.now()`）を設定してはならない**（当日開催されたと誤認されるため）。
- 日付が未確定の開催回には、必ずダミー日付 **`2099/01/01`** および `"isDateUnconfirmed": True` フラグを設定する。
- 公開ポータルでは「**開催日不明（要確認）**」と表示され、未来日付であるため過去集計（過去1年間開催数カウント等）や過去期間フィルターには混入しない。

---

## 5. 通信・スクレイピング規範

### 5.1. レートリミット（スロットリング: 0.35秒以上）
- 行政機関等の同一ドメインに対するスクレイピングやURL疎通確認時は、サーバーへの過度な負荷軽減およびAkamai/WAFによる403ブロック防止のため、**リクエスト間に必ず最低0.35秒以上（0.35〜1.0秒程度）のスリープ**を設ける。

### 5.2. 標準ブラウザヘッダー
- 行政サイトへのアクセス時は、非標準のBot User-Agentを避け、常に最新の標準ブラウザ（Chrome最新相当）の `User-Agent`, `Accept`, `Accept-Language`, `Sec-Ch-Ua` ヘッダーを設定してアクセスする。

### 5.3. 文字コード自動判定
- 日本語行政サイトの取得時は、`Content-Type`、HTML `<meta charset>`、バイト列試行（UTF-8 ➔ CP932/Shift_JIS ➔ EUC-JP）による動的判定を行い、文字化け（`ȁbʐMRc` 等）を根絶する。

---

## 6. 開発プロセスとコミュニケーション

### 6.1. 実装計画（Implementation Plan）
- コードやデータの修正作業を行う前に、必ず `implementation_plan.md` を作成・提示する。
- 提示時はアーティファクトメタデータに `RequestFeedback: true` を指定し、UI上に **Proceedボタン** を表示させる。
- ユーザーの明示的承認（Proceed）を得るまで、本番コードの修正に着手してはならない。

### 6.2. 成果報告（Walkthrough）
- 専用フィーチャーブランチでの開発・テスト完了後、修正内容と検証結果を `walkthrough.md` で報告する。
- 成果報告確認後、ユーザーの指示（「マージしてください」「OK」等）を得てから `main` ブランチへマージする（マージ時に Implementation Plan を再作成する必要はない）。

---

## 7. 禁止用語・NG表現集（厳格）

| 禁止表現 / NG | 理由 | 正しい確定用語 |
| :--- | :--- | :--- |
| **会議・開催回** | 2つの語を中黒で曖昧に併記している | **開催回** |
| **資料・配付資料** | 2つの語を中黒で曖昧に併記している | **配付資料** |
| **会議**（単体での曖昧な使用） | 「会議体（親）」か「開催回（子）」かが区別できない | **会議体** または **開催回** |
| **会議体・会議** | 2つの語を中黒で曖昧に併記している | **会議体** または **開催回** |
| **本日日付フォールバック** | 日付未取得時に当日日付をセットすると誤認を生む | **開催日不明（2099/01/01）** |
| **未承認マージ** | ユーザーの指示なく `main` に直接反映することは厳禁 | **成果報告後のユーザー指示でマージ** |
